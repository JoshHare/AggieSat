<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Project Member</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
  <div class="container">
    <h1>Add Project Member</h1>
    <%= form_with(model: @project_member, url: create_member_project_path(@project), method: :post, local: true) do |form| %>
      <div class="form-group">
        <h3><%= form.label :user_id %></h3>
        <%= form.text_field :user_id, class: 'form-control' %>
      </div>
      
      <%= hidden_field_tag :project_id, @project.id %>
    
      <%= form.submit "Add Member", class: 'btn btn-primary' %>
      <%= link_to "Cancel", edit_project_path(@project), class: 'btn btn-secondary' %>
    <% end %>
        
  </div>
</body>
</html>
