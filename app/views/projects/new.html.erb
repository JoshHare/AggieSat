<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Project Member</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
  <div class="container">
    <h1>Create New Project</h1>

    <%= form_with(model: @project, url: projects_path, method: :post, local: true) do |form| %>
        <div class="form-group">
            <%= form.label :project_name, class: "font-weight-bold" %>
            <%= form.text_field :project_name, class: "form-control" %>
        </div>

        <div class="form-group">
            <h3><%= form.label :leader_id %></h3>
            <%= form.select :leader_id, User.all.map { |user| [user.full_name, user.uid] }, {}, { class: 'form-control' } %>
        </div>

        <div class="actions">
            <%= form.submit "Create Project", class: "btn btn-primary" %>
            <%= link_to "Cancel", projects_path, class: 'btn btn-secondary' %>
        </div>
    <% end %>
  </div>
</body>
</html>
