<h1>Delete Project Confirmation</h1>
<div class="container mt-4">
  <div class="row">
    <div class="col-md-15">
        <div class="text-center">
            <h2>Are you sure you want to PERMANENTLY delete "<%= @project.project_name %>"?</h2>

            <%= button_to "Confirm Delete", project_path(@project), method: :delete, class: "btn btn-danger", data: { confirm: 'This action cannot be undone. Are you sure?' } %>

            <%= link_to "Cancel", project_path(@project), class: "btn btn-secondary mt-3" %>
        </div>
    </div>
</div>